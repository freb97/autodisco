{
  "name": "autodisco",
  "type": "module",
  "version": "0.0.1",
  "description": "Automatically typed fetch client for any API",
  "license": "MIT",
  "repository": "freb97/autodisco",
  "exports": {
    ".": "./dist/index.mjs"
  },
  "types": "./dist/index.d.mts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "obuild",
    "build:test": "obuild && node dist/index.mjs",
    "dev": "vitest dev",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "prepack": "bun run build",
    "release": "bun test && npm publish && git push --follow-tags",
    "test": "vitest run --coverage",
    "typecheck": "tsc --noEmit --skipLibCheck"
  },
  "dependencies": {
    "consola": "^3.4.2",
    "quicktype-core": "^23.2.6",
    "ufo": "^1.6.1",
    "zod": "^4.1.12",
    "zod-openapi": "^5.4.3"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^6.0.0",
    "@vitest/coverage-v8": "^3.2.4",
    "eslint": "^9.37.0",
    "obuild": "^0.2.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
