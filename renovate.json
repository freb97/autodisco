{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "group:allNonMajor",
    "schedule:monthly",
    ":maintainLockFilesMonthly"
  ],
  "rangeStrategy": "bump",
  "commitMessagePrefix": "chore:",
  "packageRules": [
    {
      "description": "Automerge non-major updates once CI is green.",
      "matchUpdateTypes": ["minor", "patch", "pin", "digest", "lockFileMaintenance"],
      "automerge": true
    },
    {
      "description": "Majors always get a human-reviewed PR.",
      "matchUpdateTypes": ["major"],
      "automerge": false
    },
    {
      "description": "TypeScript 7 is not usable yet: typescript-eslint refuses to load against TS >= 7.0 (typescript-eslint#10940) and openapi-typescript breaks against the new compiler API. Remove this rule once both support TS 7.",
      "matchPackageNames": ["typescript"],
      "allowedVersions": "<7"
    }
  ]
}
